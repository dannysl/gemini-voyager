<!-- lang:en -->

### New

- **Prompt templates**: Write `{{name}}` anywhere in a saved prompt and Voyager asks for those values before it sends. The fields sit inline in the sentence they belong to, and the slash menu resolves them the same way.
- **Sent prompts**: A prompt you sent no longer fills the turn with template text. It collapses to a chip carrying its name — open it to read what was actually sent, with the values you filled in marked, and anything you typed yourself stays visible either way.
- **Completion while you type**: After `/`, the rest of the selected prompt's name is drawn past the caret. Tab takes it, Enter inserts the prompt.
- **Change a value before sending**: Hover a prompt you have already inserted and edit what you filled in, right in the preview.

### Fixes

- **Folder leftovers**: Deleting a chat in Gemini now clears the matching folder item, so dead links no longer linger.
- **Reading position** _(off by default)_: With Prevent auto-scroll to bottom on, a finished reply no longer yanks you back to the bottom while you read earlier turns.
- **Chat width**: On Gemini's new layout, chat width and the edit box can be set separately. The composer is no longer locked to the chat width.
- **ChatGPT export button** _(off by default)_: The export button no longer covers ChatGPT's Share and more menu.
- **Gemini edit box**: On Safari and Firefox the Cancel and Update buttons sit under the edit box again instead of jumping to the far left. With the edit width slider on, the box no longer overhangs that row.
- **Prompt preview**: The hover preview renders Markdown instead of showing raw `#` and `-`, and now matches the panel's own styling.
- **Prompt accent colour**: A custom accent keeps its saturation and no longer snaps back to the default green on hover.
- **Folder dialogs**: Editing folder instructions or choosing a move target survives Gemini rebuilding its sidebar, so what you typed is no longer lost.
- **Folders on Firefox**: A failed account lookup is retried, so the panel no longer comes up empty and silently drops folders you add.
- **Preview placement**: The panel's hover preview sits beside the panel instead of lying across the list it belongs to, and a composer prompt's preview is centred just above it.
- **Long values**: A long value wraps with the sentence it sits in, rather than pushing its card into a sideways scroll.
- **Timeline surfaces**: The timeline's hover preview and context menu use the same palette and type as the prompt panel, instead of their own darker blue.

---

> _"Anyone can cook." (Gusteau, 《Ratatouille》)_

<!-- lang:zh -->

### 新增

- **提示词模板**：在保存的提示词里写 `{{名称}}`，选用时 Voyager 会先让你填值再发送。填空框就嵌在它所属的句子里，斜杠菜单走同一套解析。
- **已发送的提示词**：发出去之后不再是一整屏模板文字，而是一枚写着提示词名字的胶囊——点开看实际发了什么，你填进去的值有高亮，你自己补的那句话两种状态下都在。
- **输入时补全**：打完 `/` 之后，选中提示词剩下的名字会接在光标后面。Tab 采纳，回车插入。
- **发送前还能改**：鼠标停在已插入的提示词上，直接在预览里改填进去的值。

### 修复

- **文件夹残留**：在 Gemini 里删除对话后，文件夹中的对应条目会跟着清掉，不再留下点不开的死链。
- **阅读位置** _（默认关闭）_：打开「防自动跳转」后，生成结束也不会把你从上文拽回底部。
- **对话宽度**：Gemini 新布局下，对话宽度和编辑输入框可以分开调，编辑框不再被对话宽度锁死。
- **ChatGPT 导出按钮** _（默认关闭）_：导出按钮不再挡住 ChatGPT 顶部的分享和更多菜单。
- **Gemini 编辑框**：Safari 和 Firefox 上，取消和更新按钮回到编辑框下方，不再跑到最左边。打开编辑框宽度滑块后，编辑框也不再比按钮行宽出一截。
- **提示词预览**：悬停预览会渲染 Markdown，不再原样显示 `#` 和 `-`，样式也与面板统一。
- **提示词主题色**：自定义主题色不再掉饱和度，悬停时也不会跳回默认的绿色。
- **文件夹弹窗**：编辑文件夹指令或选择移动目标时，Gemini 重建侧边栏不会再把弹窗连同已输入的内容一起关掉。
- **Firefox 下的文件夹**：账号识别失败会自动重试，面板不再显示为空，也不会把你新建的文件夹悄悄丢掉。
- **预览位置**：提示词面板的悬停预览挪到面板旁边，不再横在它所属的列表上；输入框里提示词的预览居中贴在它正上方。
- **长内容**：过长的值跟着所在的句子换行，不再把卡片撑出横向滚动条。
- **时间线浮层**：时间线的悬停预览和右键菜单改用提示词面板同一套配色和字体，不再是自成一格的偏蓝深色。

---

> _「人人都能做菜。」（古斯多，《美食总动员》）_

<!-- lang:zh_TW -->

### 新增

- **提示詞範本**：在儲存的提示詞裡寫 `{{名稱}}`，選用時 Voyager 會先讓你填值再送出。填空框就嵌在它所屬的句子裡，斜線選單走同一套解析。
- **已送出的提示詞**：送出後不再是一整螢幕的範本文字，而是一枚寫著提示詞名稱的膠囊——點開看實際送了什麼，你填進去的值有標示，你自己補的那句話兩種狀態下都在。
- **輸入時補全**：打完 `/` 之後，選中提示詞剩下的名稱會接在游標後面。Tab 採納，Enter 插入。
- **送出前還能改**：滑鼠停在已插入的提示詞上，直接在預覽裡改填進去的值。

### 修復

- **資料夾殘留**：在 Gemini 裡刪除對話後，資料夾中的對應項目會跟著清掉，不再留下打不開的死連結。
- **閱讀位置** _（預設關閉）_：開啟「防自動跳轉」後，生成結束也不會把你從上文拽回底部。
- **對話寬度**：Gemini 新版面下，對話寬度和編輯輸入框可以分開調整，編輯框不再被對話寬度鎖死。
- **ChatGPT 匯出按鈕** _（預設關閉）_：匯出按鈕不再擋住 ChatGPT 頂部的分享和更多選單。
- **Gemini 編輯框**：Safari 和 Firefox 上，取消和更新按鈕回到編輯框下方，不再跑到最左邊。開啟編輯框寬度滑桿後，編輯框也不再比按鈕列寬出一截。
- **提示詞預覽**：停留預覽會渲染 Markdown，不再原樣顯示 `#` 和 `-`，樣式也與面板一致。
- **提示詞主題色**：自訂主題色不再掉飽和度，停留時也不會跳回預設的綠色。
- **資料夾對話框**：編輯資料夾指示或選擇移動目標時，Gemini 重建側邊欄不會再把對話框連同已輸入的內容一起關掉。
- **Firefox 下的資料夾**：帳號辨識失敗會自動重試，面板不再顯示為空，也不會把你新建的資料夾悄悄丟掉。
- **預覽位置**：提示詞面板的停留預覽挪到面板旁邊，不再橫在它所屬的清單上；輸入框裡提示詞的預覽置中貼在它正上方。
- **長內容**：過長的值會跟著所在的句子換行，不再把卡片撐出水平捲軸。
- **時間軸浮層**：時間軸的停留預覽和右鍵選單改用提示詞面板同一套配色與字體，不再是自成一格的偏藍深色。

---

> _「人人都能做菜。」（古斯圖，《料理鼠王》）_

<!-- lang:ja -->

### 新機能

- **プロンプトテンプレート**：保存したプロンプトに `{{name}}` と書いておくと、送信前にその値を尋ねます。入力欄は文中のあるべき位置にそのまま並び、スラッシュメニューも同じ仕組みで解決します。
- **送信したプロンプト**：送信後もテンプレート全文が並ぶことはなくなり、名前を書いたチップにまとまります。開けば実際に送った内容が読め、入力した値には印がつき、自分で書き足した文はどちらの状態でも見えたままです。
- **入力中の補完**：`/` のあと、選択中のプロンプトの残りの名前がカーソルの先に薄く表示されます。Tab で確定、Enter で挿入。
- **送信前に値を直す**：挿入済みのプロンプトにカーソルを合わせると、プレビューの中で入力した値をそのまま編集できます。

### 修正

- **フォルダーの取り残し**：Gemini でチャットを削除すると、フォルダー内の対応する項目も消えるようになり、開けない死んだリンクが残らなくなりました。
- **読書位置** _(既定でオフ)_：「自動スクロール防止」をオンにすると、生成が終わっても、上のほうを読んでいる最中に一番下へ引き戻されません。
- **チャット幅**：Gemini の新しいレイアウトでは、チャット幅と編集入力欄を別々に設定できます。入力欄がチャット幅に固定されなくなりました。
- **ChatGPT のエクスポートボタン** _(既定でオフ)_：エクスポートボタンが ChatGPT 上部の共有やその他メニューを覆わなくなりました。
- **Gemini の編集ボックス**：Safari と Firefox で、キャンセルと更新のボタンが左端へ飛ばず、編集ボックスの下に戻りました。編集幅スライダーを有効にしても、ボックスがボタン列からはみ出しません。
- **プロンプトのプレビュー**：ホバープレビューが `#` や `-` をそのまま出さず Markdown として描画するようになり、見た目もパネルに揃いました。
- **プロンプトのアクセント色**：カスタムのアクセント色が彩度を失わず、ホバーで既定の緑に戻ることもなくなりました。
- **フォルダーのダイアログ**：フォルダー指示の編集や移動先の選択が、Gemini のサイドバー再構築で閉じられなくなり、入力した内容が消えません。
- **Firefox のフォルダー**：アカウント解決に失敗しても再試行するため、パネルが空のままになったり、追加したフォルダーが黙って失われたりしません。
- **プレビューの位置**：パネルのホバープレビューがリストの上に重ならず、パネルの横に出るようになりました。入力欄のプロンプトのプレビューは、その真上に中央揃えで表示されます。
- **長い値**：長い値は置かれている文と一緒に折り返し、カードを横スクロールさせなくなりました。
- **タイムラインの面**：タイムラインのホバープレビューとコンテキストメニューが、独自の青みがかった暗色ではなく、プロンプトパネルと同じ配色と書体になりました。

---

> _「誰でも料理はできる。」（グストー、『レミーのおいしいレストラン』）_

<!-- lang:fr -->

### Nouveautés

- **Modèles de prompt** : écrivez `{{nom}}` dans un prompt enregistré et Voyager demande ces valeurs avant l’envoi. Les champs se placent dans la phrase à laquelle ils appartiennent, et le menu slash utilise le même analyseur.
- **Prompts envoyés** : un prompt envoyé ne remplit plus le tour de texte de gabarit. Il se réduit à une pastille portant son nom — ouvrez-la pour lire ce qui a réellement été envoyé, les valeurs que vous avez saisies y sont mises en évidence, et ce que vous avez écrit vous-même reste visible dans les deux états.
- **Complétion pendant la saisie** : après `/`, la fin du nom du prompt sélectionné s'affiche au-delà du curseur. Tab l'accepte, Entrée insère le prompt.
- **Modifier une valeur avant l'envoi** : survolez un prompt déjà inséré et modifiez ce que vous aviez saisi, directement dans l'aperçu.

### Corrections

- **Entrées de dossier orphelines** : supprimer une conversation dans Gemini retire désormais l’entrée correspondante du dossier, sans laisser de liens morts.
- **Position de lecture** _(désactivé par défaut)_ : avec « Empêcher le défilement auto » activé, une réponse terminée ne vous ramène plus en bas pendant que vous relisez plus haut.
- **Largeur du chat** : dans la nouvelle mise en page de Gemini, la largeur du chat et celle de la zone d’édition se règlent séparément. La zone de saisie n’est plus verrouillée sur la largeur du chat.
- **Bouton d’export ChatGPT** _(désactivé par défaut)_ : le bouton d’export ne recouvre plus Partager ni le menu Autres en haut de ChatGPT.
- **Zone d’édition de Gemini** : sur Safari et Firefox, les boutons Annuler et Mettre à jour reviennent sous la zone d’édition au lieu de filer tout à gauche. Avec le curseur de largeur d’édition activé, la zone ne déborde plus de cette rangée.
- **Aperçu des prompts** : l’aperçu au survol rend le Markdown au lieu d’afficher `#` et `-` bruts, et adopte le style du panneau.
- **Couleur d’accent des prompts** : une couleur personnalisée garde sa saturation et ne revient plus au vert par défaut au survol.
- **Boîtes de dialogue des dossiers** : modifier les instructions d’un dossier ou choisir une destination survit à la reconstruction de la barre latérale de Gemini ; le texte saisi n’est plus perdu.
- **Dossiers sur Firefox** : une résolution de compte échouée est réessayée, donc le panneau ne s’affiche plus vide et n’abandonne plus en silence les dossiers que vous ajoutez.
- **Position de l'aperçu** : l'aperçu au survol du panneau se place à côté de celui-ci au lieu de s'étaler sur la liste, et l'aperçu d'un prompt du champ de saisie est centré juste au-dessus de lui.
- **Valeurs longues** : une valeur longue passe à la ligne avec la phrase qui l'entoure, au lieu de forcer sa carte à défiler horizontalement.
- **Surfaces de la timeline** : l'aperçu au survol et le menu contextuel de la timeline reprennent la palette et la typographie du panneau de prompts, au lieu de leur propre bleu sombre.

---

> _« Tout le monde peut cuisiner. » (Gusteau, 《Ratatouille》)_

<!-- lang:es -->

### Novedades

- **Plantillas de prompt**: escribe `{{nombre}}` en un prompt guardado y Voyager te pedirá esos valores antes de enviarlo. Los campos quedan dentro de la frase a la que pertenecen, y el menú de barra usa el mismo analizador.
- **Prompts enviados**: un prompt enviado ya no llena el turno con el texto de la plantilla. Se reduce a una pastilla con su nombre: ábrela para leer lo que se envió realmente, con los valores que rellenaste resaltados, y lo que escribiste tú sigue visible en ambos estados.
- **Completado mientras escribes**: tras `/`, el resto del nombre del prompt seleccionado aparece más allá del cursor. Tab lo acepta, Intro inserta el prompt.
- **Cambiar un valor antes de enviar**: pasa el cursor por un prompt ya insertado y edita lo que rellenaste, en la propia vista previa.

### Correcciones

- **Restos en carpetas**: al borrar un chat en Gemini, la entrada correspondiente de la carpeta también se elimina, sin dejar enlaces rotos.
- **Posición de lectura** _(desactivado por defecto)_: con «Evitar el desplazamiento automático» activado, una respuesta terminada ya no te arrastra al final mientras lees más arriba.
- **Ancho del chat**: en el nuevo diseño de Gemini, el ancho del chat y el del cuadro de edición se ajustan por separado. El compositor ya no queda bloqueado al ancho del chat.
- **Botón de exportar de ChatGPT** _(desactivado por defecto)_: el botón de exportar ya no tapa Compartir ni el menú Más de ChatGPT.
- **Cuadro de edición de Gemini**: en Safari y Firefox los botones Cancelar y Actualizar vuelven a quedar bajo el cuadro en lugar de irse al extremo izquierdo. Con el control de ancho de edición activado, el cuadro ya no sobresale de esa fila.
- **Vista previa de prompts**: la vista previa al pasar el cursor representa Markdown en vez de mostrar `#` y `-` en crudo, y sigue el estilo del panel.
- **Color de acento de prompts**: un acento personalizado conserva su saturación y ya no vuelve al verde por defecto al pasar el cursor.
- **Diálogos de carpetas**: editar las instrucciones de una carpeta o elegir un destino sobrevive a que Gemini reconstruya su barra lateral, sin perder lo escrito.
- **Carpetas en Firefox**: si falla la resolución de la cuenta se reintenta, así el panel ya no aparece vacío ni descarta en silencio las carpetas que añades.
- **Posición de la vista previa**: la vista previa del panel se coloca junto a él en lugar de tumbarse sobre la lista, y la de un prompt del cuadro de escritura queda centrada justo encima.
- **Valores largos**: un valor largo se ajusta con la frase en la que está, en vez de obligar a su tarjeta a desplazarse en horizontal.
- **Superficies de la línea de tiempo**: la vista previa y el menú contextual de la línea de tiempo usan la paleta y la tipografía del panel de prompts, en lugar de su propio azul oscuro.

---

> _«Cualquiera puede cocinar.» (Gusteau, 《Ratatouille》)_

<!-- lang:pt -->

### Novidades

- **Modelos de prompt**: escreva `{{nome}}` num prompt guardado e o Voyager pede esses valores antes de enviar. Os campos ficam dentro da frase a que pertencem, e o menu de barra usa o mesmo analisador.
- **Prompts enviados**: um prompt enviado deixa de encher a mensagem com o texto do modelo. Fica reduzido a uma cápsula com o seu nome — abra-a para ler o que foi mesmo enviado, com os valores que preencheu destacados, e o que escreveu continua visível nos dois estados.
- **Conclusão enquanto escreve**: depois de `/`, o resto do nome do prompt selecionado aparece para lá do cursor. Tab aceita, Enter insere o prompt.
- **Mudar um valor antes de enviar**: passe o rato sobre um prompt já inserido e edite o que preencheu, na própria pré-visualização.

### Correções

- **Sobras nas pastas**: apagar um chat no Gemini agora remove o item correspondente da pasta, sem deixar ligações mortas.
- **Posição de leitura** _(desativado por predefinição)_: com «Prevenir rolamento automático» ligado, uma resposta concluída já não o puxa para o fundo enquanto lê mais acima.
- **Largura do chat**: no novo esquema do Gemini, a largura do chat e a da caixa de edição ajustam-se em separado. A caixa de escrita deixa de ficar presa à largura do chat.
- **Botão de exportar do ChatGPT** _(desativado por predefinição)_: o botão de exportar já não cobre Partilhar nem o menu Mais no topo do ChatGPT.
- **Caixa de edição do Gemini**: no Safari e no Firefox os botões Cancelar e Atualizar voltam a ficar por baixo da caixa em vez de saltarem para a esquerda. Com o controlo de largura de edição ligado, a caixa deixa de ultrapassar essa linha.
- **Pré-visualização de prompts**: a pré-visualização ao passar o rato apresenta Markdown em vez de mostrar `#` e `-` em bruto, e segue o estilo do painel.
- **Cor de destaque dos prompts**: uma cor personalizada mantém a saturação e já não volta ao verde predefinido ao passar o rato.
- **Caixas de diálogo das pastas**: editar as instruções de uma pasta ou escolher o destino sobrevive à reconstrução da barra lateral do Gemini, sem perder o que escreveu.
- **Pastas no Firefox**: uma resolução de conta falhada é repetida, por isso o painel deixa de aparecer vazio e de descartar em silêncio as pastas que adiciona.
- **Posição da pré-visualização**: a pré-visualização do painel fica ao lado dele em vez de se deitar sobre a lista, e a de um prompt da caixa de escrita fica centrada logo acima dele.
- **Valores longos**: um valor longo passa à linha com a frase onde está, em vez de obrigar o cartão a deslizar na horizontal.
- **Superfícies da linha do tempo**: a pré-visualização e o menu de contexto da linha do tempo passam a usar a paleta e a tipografia do painel de prompts, e não o seu próprio azul escuro.

---

> _«Qualquer um pode cozinhar.» (Gusteau, 《Ratatouille》)_

<!-- lang:ar -->

### الجديد

- **قوالب المطالبات**: اكتب `{{الاسم}}` داخل مطالبة محفوظة، وسيطلب منك Voyager هذه القيم قبل الإرسال. تظهر الحقول داخل الجملة التي تنتمي إليها، وقائمة الشرطة المائلة تستخدم المحلّل نفسه.
- **المطالبات المُرسَلة**: لم تعد المطالبة المرسَلة تملأ الرسالة بنص القالب، بل تنطوي إلى شارة تحمل اسمها — افتحها لقراءة ما أُرسل فعلًا، والقيم التي أدخلتها مميّزة بداخلها، وما كتبته بنفسك يبقى ظاهرًا في الحالتين.
- **إكمال أثناء الكتابة**: بعد `/` يظهر بقية اسم المطالبة المحدّدة خلف المؤشر. يقبله Tab، ويُدرج Enter المطالبة.
- **تعديل قيمة قبل الإرسال**: مرّر المؤشر فوق مطالبة أدرجتها وعدّل ما ملأته، من داخل المعاينة نفسها.

### الإصلاحات

- **بقايا المجلدات**: حذف محادثة في Gemini يزيل الآن البند المطابق من المجلد، فلا تبقى روابط ميتة.
- **موضع القراءة** _(مُعطَّل افتراضيًا)_: مع تفعيل «منع التمرير التلقائي»، لن تعيدك إجابة منتهية إلى الأسفل وأنت تقرأ ما فوقها.
- **عرض المحادثة**: في تخطيط Gemini الجديد يمكن ضبط عرض المحادثة وصندوق التحرير كلٌّ على حدة، ولم يعد صندوق الإدخال مقيدًا بعرض المحادثة.
- **زر تصدير ChatGPT** _(مُعطَّل افتراضيًا)_: لم يعد زر التصدير يغطي «مشاركة» وقائمة المزيد أعلى ChatGPT.
- **صندوق التحرير في Gemini**: على Safari و Firefox عاد زرّا الإلغاء والتحديث أسفل صندوق التحرير بدل الانزياح إلى أقصى الجانب. ومع تفعيل شريط عرض التحرير، لم يعد الصندوق يتجاوز صف الأزرار.
- **معاينة المطالبات**: تعرض معاينة التمرير Markdown بدل إظهار `#` و `-` كما هي، وصار مظهرها متسقًا مع اللوحة.
- **لون تمييز المطالبات**: يحتفظ اللون المخصص بتشبّعه، ولم يعد يعود إلى الأخضر الافتراضي عند التمرير.
- **نوافذ المجلدات**: تحرير تعليمات مجلد أو اختيار وجهة النقل يصمد أمام إعادة بناء Gemini للشريط الجانبي، فلا يضيع ما كتبته.
- **المجلدات على Firefox**: تُعاد محاولة تحديد الحساب عند فشلها، فلا تظهر اللوحة فارغة ولا تُفقد المجلدات التي تضيفها بصمت.
- **موضع المعاينة**: صارت معاينة اللوحة تظهر بجانبها بدل أن تمتد فوق القائمة نفسها، ومعاينة مطالبة في صندوق الكتابة تظهر فوقها مباشرة وفي المنتصف.
- **القيم الطويلة**: تلتف القيمة الطويلة مع الجملة التي تقع فيها، بدل أن تدفع البطاقة إلى تمرير أفقي.
- **أسطح المخطط الزمني**: صارت معاينة المخطط الزمني وقائمته السياقية تستخدمان ألوان وخطوط لوحة المطالبات نفسها، بدل الأزرق الداكن الخاص بهما.

---

> _«يستطيع أي أحد أن يطبخ.» (غوستو، 《Ratatouille》)_

<!-- lang:ko -->

### 새 기능

- **프롬프트 템플릿**: 저장한 프롬프트에 `{{이름}}`을 써 두면 보내기 전에 그 값을 물어봅니다. 입력란은 해당 문장 안에 그대로 놓이고, 슬래시 메뉴도 같은 방식으로 처리합니다.
- **보낸 프롬프트**: 보낸 뒤에도 템플릿 전문이 깔리지 않고, 이름이 적힌 칩으로 접힙니다. 열면 실제로 보낸 내용을 볼 수 있고 채워 넣은 값에는 표시가 붙으며, 직접 덧붙인 문장은 두 상태 모두에서 그대로 보입니다.
- **입력 중 자동완성**: `/` 뒤에 선택된 프롬프트의 남은 이름이 커서 뒤에 흐리게 나타납니다. Tab으로 받아들이고, Enter로 삽입합니다.
- **보내기 전에 값 수정**: 이미 삽입한 프롬프트에 마우스를 올리면 미리보기 안에서 채운 값을 바로 고칠 수 있습니다.

### 수정

- **폴더에 남은 항목**: Gemini에서 채팅을 삭제하면 폴더의 해당 항목도 함께 지워져, 열리지 않는 죽은 링크가 남지 않습니다.
- **읽는 위치** _(기본값 꺼짐)_: 「자동 스크롤 방지」를 켜 두면, 생성이 끝나도 위쪽을 읽는 중에 맨 아래로 끌어내려지지 않습니다.
- **채팅 너비**: Gemini의 새 레이아웃에서는 채팅 너비와 편집 입력란을 따로 맞출 수 있습니다. 입력란이 채팅 너비에 묶이지 않습니다.
- **ChatGPT 내보내기 버튼** _(기본값 꺼짐)_: 내보내기 버튼이 ChatGPT 상단의 공유 및 더보기 메뉴를 가리지 않습니다.
- **Gemini 편집 상자**: Safari와 Firefox에서 취소·업데이트 버튼이 왼쪽 끝으로 튀지 않고 편집 상자 아래로 돌아왔습니다. 편집 너비 슬라이더를 켜도 상자가 버튼 줄 밖으로 삐져나오지 않습니다.
- **프롬프트 미리보기**: 마우스를 올렸을 때의 미리보기가 `#`, `-`를 그대로 보여주지 않고 Markdown으로 렌더링하며, 패널과 같은 스타일을 따릅니다.
- **프롬프트 강조색**: 직접 고른 강조색이 채도를 잃지 않고, 마우스를 올려도 기본 초록으로 되돌아가지 않습니다.
- **폴더 대화상자**: 폴더 지침을 수정하거나 이동 위치를 고르는 중에 Gemini가 사이드바를 다시 그려도 창이 닫히지 않아, 입력한 내용이 사라지지 않습니다.
- **Firefox의 폴더**: 계정 확인에 실패하면 다시 시도하므로, 패널이 비어 보이거나 새로 만든 폴더가 조용히 사라지지 않습니다.
- **미리보기 위치**: 패널의 호버 미리보기가 목록 위를 가로지르지 않고 패널 옆에 놓이며, 입력창 프롬프트의 미리보기는 바로 위에 가운데로 붙습니다.
- **긴 값**: 긴 값이 놓인 문장과 함께 줄바꿈되어, 카드가 가로로 밀려나지 않습니다.
- **타임라인 표면**: 타임라인의 호버 미리보기와 컨텍스트 메뉴가 자체적인 짙은 파랑 대신 프롬프트 패널과 같은 색과 서체를 씁니다.

---

> _"누구나 요리할 수 있다." (구스토, 《라따뚜이》)_

<!-- lang:ru -->

### Новое

- **Шаблоны промптов**: напишите `{{имя}}` в сохранённом промпте, и Voyager спросит эти значения перед отправкой. Поля стоят прямо в том предложении, которому принадлежат, а слэш-меню разбирает их так же.
- **Отправленные промпты**: отправленный промпт больше не заполняет сообщение текстом шаблона, а сворачивается в плашку с его названием — откройте её, чтобы прочитать отправленное; подставленные значения выделены, а написанное вами видно в обоих состояниях.
- **Подсказка при вводе**: после `/` остаток названия выбранного промпта показывается за курсором. Tab принимает его, Enter вставляет промпт.
- **Правка значения до отправки**: наведите курсор на уже вставленный промпт и измените подставленное прямо в предпросмотре.

### Исправления

- **Остатки в папках**: удаление чата в Gemini теперь убирает и соответствующий пункт папки, мёртвые ссылки больше не остаются.
- **Место чтения** _(по умолчанию выключено)_: если включено «Предотвращение автопрокрутки», законченный ответ больше не сматывает вас вниз, пока вы читаете выше.
- **Ширина чата**: в новой вёрстке Gemini ширину чата и поля редактирования можно задавать отдельно. Поле ввода больше не привязано к ширине чата.
- **Кнопка экспорта ChatGPT** _(по умолчанию выключено)_: кнопка экспорта больше не перекрывает «Поделиться» и меню «Ещё» в шапке ChatGPT.
- **Поле редактирования Gemini**: в Safari и Firefox кнопки «Отмена» и «Обновить» снова стоят под полем, а не уезжают к левому краю. При включённом ползунке ширины поле больше не выступает за этот ряд.
- **Предпросмотр промптов**: всплывающий предпросмотр рендерит Markdown, а не показывает `#` и `-` как есть, и оформлен в стиле панели.
- **Акцентный цвет промптов**: выбранный цвет сохраняет насыщенность и больше не возвращается к зелёному по умолчанию при наведении.
- **Диалоги папок**: правка инструкций папки и выбор места перемещения переживают перестройку боковой панели Gemini — набранный текст не пропадает.
- **Папки в Firefox**: неудачное определение аккаунта повторяется, поэтому панель больше не открывается пустой и не теряет молча добавленные папки.
- **Положение предпросмотра**: предпросмотр панели встаёт рядом с ней, а не поперёк списка, а предпросмотр промпта из поля ввода располагается по центру прямо над ним.
- **Длинные значения**: длинное значение переносится вместе с фразой, в которой стоит, и не растягивает карточку в горизонтальную прокрутку.
- **Поверхности таймлайна**: предпросмотр и контекстное меню таймлайна используют ту же палитру и шрифт, что и панель промптов, вместо собственного тёмно-синего.

---

> _«Готовить может каждый.» (Гюсто, 《Рататуй》)_
